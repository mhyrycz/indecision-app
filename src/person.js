export const isAdult = (x) => {
  if (x >= 18) {
    return true }
  else {
    return false
  }
  };

export const canDrink = (x) => (x>=18) ? true : false;

export const canDrink2 = (age) => age >= 18;
